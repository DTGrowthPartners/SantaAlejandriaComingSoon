import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Configuración"
      description="Usuarios, roles, datos del hotel y credenciales de Bold. En construcción."
    />
  );
}
